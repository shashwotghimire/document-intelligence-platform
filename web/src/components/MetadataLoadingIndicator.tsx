import LoadingDots from "@/components/LoadingDots";

const MetadataLoadingIndicator = () => (
  <div className="mt-3 border-t border-border/60 pt-2 text-xs text-muted-foreground">
    <LoadingDots />
  </div>
);

export default MetadataLoadingIndicator;
